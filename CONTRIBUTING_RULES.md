# 專案分支使用指南 (Branch Workflow Guide)

歡迎來到我們的專案團隊！為了讓大家能放心地合作開發，不用擔心不小心把錯誤的程式碼或寫到一半的文件覆蓋掉正式版本，我們採用了「分支 (Branch)」的工作模式。

簡單來說，分支就像是專案的「平行時空」，你可以盡情在自己的時空裡修改，確認沒問題後再把你的時空和大家的合併起來。這份文件會教你如何正確使用分支！

## 📂 我們的核心分支架構

目前我們的專案主要分為兩個核心分支：

*   **`code` 分支**：所有**程式碼**開發、功能新增與 Bug 修復的基礎分支。
*   **`docs` 分支**：所有**文件**（如 README、操作說明、API 規格等）的基礎分支。

> **⚠️ 重要規定：**
> 為了保護專案，**絕對不要**直接把你的修改推上 (Push) `code` 或 `docs` 分支！請永遠先建立你自己的專屬分支。

---

## 🚀 新手必讀：日常工作流程 (Workflow)

無論你是要寫程式還是寫文件，請務必遵循以下四個步驟：

### 步驟一：取得最新版本 (Pull)
在開始任何工作之前，請先確保你電腦裡的檔案是最新的，避免跟別人發生衝突。
1. 在 VS Code 左下角確認你目前在哪個分支。
2. 切換到你想修改的基礎分支（寫程式請切換到 `code`，寫文件請切換到 `docs`）。
3. 點擊左下角的「同步」按鈕，或是開啟終端機輸入：
   ```bash
   git pull origin code  # 如果你要寫程式
   # 或
   git pull origin docs  # 如果你要寫文件