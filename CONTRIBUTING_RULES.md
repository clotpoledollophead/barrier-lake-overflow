# 🚀 CodeScape 新手村：協作與開發完全指南

哈囉！歡迎加入專案開發！🎉

我們使用 GitHub 的目的，就是為了給大家一個「可以無限次犯錯、無限次重來」的安全沙盒。這份指南會用最簡單的方式，告訴你我們團隊的遊戲規則。

---

## 🌍 第一堂課：什麼是「分支 (Branch)」？

想像我們的專案是一條主線劇情的「時間線」。如果大家都在同一條時間線上改東西，一定會天下大亂（例如： a 寫到一半的扣，突然被 b 存檔覆蓋掉）。

為了解決這個問題，Git 允許我們**「開分身（分支）」**。你可以複製一份一模一樣的專案到你的平行時空裡，在裡面怎麼惡搞、怎麼壞掉都沒關係。等你確認寫得很完美了，我們再把你的平行時空跟主線「合併 (Merge)」。

我們專案有 **三條神聖的主線**：
1. **`main`**：最高殿堂，這是要給外面的人看的最終成品。
2. **`code`**：開發大本營。所有關於「程式碼」的新功能、修 Bug，最後都會來到這裡。
3. **`docs`**：文件圖書館。所有關於「說明書、教學文件」的內容，都會存放在這裡。

> 🛡️ **【新手保護機制已啟動】**
> 為了保護大家的心血，這三條主線已經被鎖上了**「絕對防禦」**。
> 你**無法**直接把檔案存進這三個分支裡，系統會自動擋下你。要把程式碼放進去，你必須經過我們團隊專屬的「交作業審核流程（也就是 PR）」。

---

## 🎮 第二堂課：日常開發的 5 個標準動作

以後每次打開 VS Code 準備開始做事，請把你當成是在玩遊戲，照著以下五個步驟破關：

### 🎯 動作 1：下載最新進度 (Pull)
*   **為什麼要做？** 確保你的電腦裡擁有團隊最新的進度，才不會用舊版本的程式碼去改。
*   **怎麼做？** 
    1. 看 VS Code 左下角，確認你目前在 `code` (寫程式) 或 `docs` (寫文件) 分支。
    2. 點擊左下角的「同步按鈕」，或是打開終端機輸入：`git pull origin code`。

### 🎯 動作 2：建立你的專屬分身 (Create Branch)
*   **為什麼要做？** 因為主線被鎖住了，你必須開一個自己的平行時空來做事。
*   **怎麼做？** 
    1. 點擊 VS Code 左下角的分支名稱（例如 code）。
    2. 選擇 **+ 建立新分支 (Create new branch)**。
    3. **命名規則**：`你的名字/你要做的事`（例如：`sitong/add-tutorial-ui` 或 `zijie/fix-login-bug`）。按下 Enter，你就進入自己的時空了！

### 🎯 動作 3：開心寫扣，並「存檔紀錄」 (Commit)
*   **為什麼要做？** 在 Git 裡面，一般的 Ctrl+S 存檔不算數，你必須「打包並寫下日記 (Commit)」，Git 才會幫你記住這個進度。
*   **怎麼做？**
    1. 程式寫到一個段落後，點開 VS Code 左邊第三個圖示（原始檔控制，長得像樹枝）。
    2. 點擊更改檔案旁邊的 `+` 號（這叫暫存，Stage）。
    3. 在上面的對話框寫下你做了什麼，例如：`新增：登入按鈕的畫面`。
    4. 按下 **提交 (Commit)**。你可以重複很多次這個動作，把你的進度一段一段存起來。

### 🎯 動作 4：上傳你的分身到雲端 (Push)
*   **為什麼要做？** 剛剛的 Commit 都只存在你的個人電腦裡，如果電腦壞了就沒了。Push 就是把你的分身備份到 GitHub 雲端上。
*   **怎麼做？** 點擊 VS Code 左下角的 **發佈分支 (Publish Branch)**（一個小雲朵圖示）。

### 🎯 動作 5：提交申請單 (Pull Request, 簡稱 PR)
*   **為什麼要做？** 你的分身已經在雲端了，現在你要填寫申請單，請求管理員把你的分身「合併」回主線 (`code` 或 `docs`)。
*   **怎麼做？**
    1. 上傳成功後，去我們的 GitHub 網站。
    2. 畫面上方會亮起一個綠色按鈕 **Compare & pull request**，點下去！
    3. **檢查目標**：確認你是要把程式合併到 `code`，或是把文件合併到 `docs`，千萬別選錯囉。
    4. 寫好你想跟管理員說的話，按下 Create。

---

## 👩‍🏫 第三堂課：為什麼我的 PR 被卡住了？

當你送出 PR 申請單後，會進入審核階段。如果你發現你的 PR 無法合併，通常是因為以下幾個系統防呆機制：

1. **等待管理員核准 (Require review from Code Owners)**
   只有專案負責人（Admin）看完你的程式碼，並且親自按下 **Approve (核准)** 後，綠色的 Merge 按鈕才會亮起。別人幫你按是無效的喔！
2. **核准被取消了 (Dismiss stale approvals)**
   如果管理員已經按了 Approve，但你覺得程式怪怪的，**又自己 Push 了新的修改上去**，系統會「沒收」原本的核准。因為你交卷後又偷改答案啦！必須請管理員再看一次，確保你新加的東西沒問題。
3. **還有未解決的討論 (Require conversation resolution)**
   如果管理員在你的 PR 裡面畫重點留言（例如：「子傑，這裡的排版跑掉了，請修一下」），你必須把程式碼改好、Push 上去，然後在那個留言點擊 **Resolve conversation (已解決)**。所有留言都解決了才能過關。

> ✨ **超棒的「壓縮合併 (Squash Merge)」**
> 新手寫扣常會有很多零碎的存檔（例如 `commit: 測試`, `commit: 再測試`, `commit: 終於修好了`）。
> 別擔心！當管理員幫你合併時，系統會啟動「壓縮」魔法，把你這幾十個雜亂的草稿，濃縮成「一行乾淨的紀錄」寫進主線裡。所以你在自己的分支裡可以盡情地、放肆地 Commit！

---

## 🚑 第四堂課：新手三大保命金牌

只要記住這三句話，你就天下無敵：

1. 🛑 **絕對不要使用強制推送 (`git push -f`)！**
   如果在終端機或 VS Code 看到紅字報錯，千萬不要上網查指令然後加上 `-f` 或 `--force` 硬推。這會把伺服器上其他人的心血直接刪掉。有紅字，直接截圖丟群組！
2. 🔀 **遇到「合併衝突 (Merge Conflict)」不要慌！**
   衝突只是因為兩個人剛好改到了同一個檔案的同一行，Git 不知道該聽誰的，所以停下來問我們。請在群組呼叫大家，我們會一起用 VS Code 的介面點擊幾下，輕鬆解決它。
3. 🏠 **迷路了就重新來過**
   如果在自己的分支上把程式碼改得亂七八糟、專案跑不起來了，別緊張！因為你是在自己的平行時空，完全沒有影響到主線。最壞的打算就是把這個分支刪掉，重新從 `code` 切換一個新的分支出來，又是一條好漢！

我們是一個團隊，有任何不確定的地方，隨時發問。祝大家寫扣愉快！🚀